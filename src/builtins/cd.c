#include <err.h>
#include <stdlib.h>
#include <unistd.h>

int cd(char **args, size_t len)
{
    char *home = getenv("HOME");
    if (home)
    {

    }
    else
    {

    }
    return 0;
}